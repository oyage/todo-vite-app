/// <reference types="vite/client" />
// 上記の行は、Viteクライアントの型定義を参照するためのトリプルスラッシュディレクティブです。
// これにより、プロジェクト内で `import.meta.env` などのVite特有のグローバル変数の型情報が
// TypeScriptコンパイラに認識されるようになります。
// 通常、このファイルは手動で編集する必要はありません。Viteがプロジェクトセットアップ時に自動生成します。
